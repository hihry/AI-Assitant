"""Parse PDF resumes into structured JSON."""


def parse_resume_pdf(pdf_path: str) -> dict:
    """Extract and normalize resume data from a PDF file."""
    raise NotImplementedError("Implement resume parsing in modules/parse_resume.py")
